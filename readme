# AI Data Extraction Dashboard

A Streamlit-based dashboard that processes data using LLMs (Large Language Models) and performs web searches to extract relevant information.

## Prerequisites

- Python 3.8+
- Streamlit
- Two Groq API keys (for load balancing)
- DuckDuckGo search capabilities

## API Key Setup

1. Visit [Groq's website](https://console.groq.com) and create an account
2. Generate two API keys from your Groq dashboard:
   - First key for initial query generation
   - Second key for final processing
3. Create a `.env` file in the project root:
   ```bash
   touch .env
   ```
4. Add your Groq API keys to the `.env` file:
   ```
   GROQ_API_KEY_1=your_first_groq_api_key
   GROQ_API_KEY_2=your_second_groq_api_key
   ```

> ⚠️ **Important**: Never commit your `.env` file to version control. It's already included in `.gitignore`.

## Installation

1. Clone the repository:
   ```bash
   git clone <repository-url>
   cd <project-directory>
   ```

2. Create and activate a virtual environment:
   ```bash
   python -m venv venv
   source venv/bin/activate  # On Windows: venv\Scripts\activate
   ```

3. Install dependencies:
   ```bash
   pip install -r requirements.txt
   ```

4. Run the application:
   ```bash
   streamlit run dashboard/main.py
   ```

[Rest of README content...]